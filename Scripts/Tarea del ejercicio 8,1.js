let a = 1, b = 1;


alert( ++a ); // 2, la forma de prefijo devuelve el nuevo valor
alert( a ); // 1, la forma de sufijo devuelve el antiguo valor

alert( a ); // 2, incrementado una vez
alert( b ); // 2, incrementado una vez
